diff --git a/requirements.txt b/requirements.txt
index 39ed44f..33d90ea 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -3,7 +3,7 @@ matplotlib
 numpy
 omegaconf>=2.0.1
 onnx
-onnx-simplifier
+onnxsim
 opencv-python
 pyaml
 pycocotools
diff --git a/setup.py b/setup.py
index d2dccb8..7cf18a8 100644
--- a/setup.py
+++ b/setup.py
@@ -12,7 +12,21 @@ if __name__ == "__main__":
         author=__author__,
         author_email=__author_email__,
         keywords="deep learning",
-        packages=find_packages(exclude=("config", "tools", "demo")),
+        # Discover packages under the repo; expose the top-level `config/`
+        # directory as `nanodet.config` and include `tools/` as
+        # `nanodet.tools` so tool scripts are installed as package data.
+        packages=find_packages(exclude=("config", "demo")) + ["nanodet.config", "nanodet.tools"],
+        package_dir={
+            "nanodet.config": "config",
+            "nanodet.tools": "tools",
+        },
+        package_data={
+            # include YAMLs and nested config folders
+            "nanodet.config": ["*.yml", "convnext/*", "legacy_v0.x_configs/*", "legacy_v0.x_configs/**/*"],
+            # include tool scripts so they are part of installed package
+            "nanodet.tools": ["*.py"],
+        },
+        include_package_data=True,
         classifiers=[
             "Development Status :: Beta",
             "License :: OSI Approved :: Apache Software License",
