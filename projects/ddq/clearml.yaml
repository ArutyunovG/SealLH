# DDQ-specific ClearML configuration
clearml:
  # DDQ project identifier
  project_name: "sandbox/${project_name}"
  
  # Additional pip requirements for DDQ project (visualization-specific)
  pip:
    pip_extras:
      - mmcv==2.1.0
      - mmdet==3.3.0
      - timm==1.0.24
